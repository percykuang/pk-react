import type { Config } from 'prettier';

const config: Config = {
	semi: true,
	singleQuote: true,
	trailingComma: 'none',
	printWidth: 80,
	tabWidth: 2,
	useTabs: true,
	bracketSpacing: true
};

export default config;
